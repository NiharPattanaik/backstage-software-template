apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.app_name}}
  description: Python app to test backstage.
  annotations:
    github.com/project-slug: NiharPattanaik/${{values.app_name}}

spec:
  type: service
  owner: development
  lifecycle: experimental